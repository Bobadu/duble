# Duble - build artefacts and local files.
bin/
obj/
publish/
TestResults/

# the interface: dependencies and the bundle built from them
node_modules/
Duble.App/web/dist/

# IDE / editor
.vs/
.vscode/
.idea/
*.user
*.suo

# local interface check scripts (Duble.exe --dev --exec "…"), never part of the build
Duble.App/web/src/_dev-*.ts

# local implementation plans (working notes, not project documentation)
.plans/
